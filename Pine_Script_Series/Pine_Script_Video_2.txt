// This Pine Script® code is subject to the terms of the Mozilla Public License 2.0 at https://mozilla.org/MPL/2.0/
// © algodeltatv

//@version=6
indicator("Pine Scrit Series Viedo 2", overlay = false)
// plot(close)
// // ============= SMA =============
// sma = ta.sma(close, 9)
// plot(sma, color = color.green)

// // ============= EMA Crossover and Crossunder =============
// ema1 = ta.ema(close, 9)
// ema2 = ta.ema(close, 26)
// plot(ema1, color = color.green)
// plot(ema2, color = color.orange)

// crossOver = ta.crossover(ema1, ema2)
// crossUnder = ta.crossunder(ema1, ema2)

// plotshape(crossOver, title = "Cross Over", style = shape.arrowup, color = color.teal, location = location.belowbar, size = size.huge)
// plotshape(crossUnder, title = "Cross Under", style = shape.arrowdown, color = color.red, location = location.belowbar, size = size.huge)

// // ============= Rsi =============
// // Note - In this indicator you have to put the overlay = false in the main indicator function
// rsi = ta.rsi(close, 14)
// upperBand = plot(70, title = "Upper Band", linestyle = plot. linestyle_dashed)
// middleBand = plot(50, title = "Middle Band", linestyle = plot. linestyle_dashed)
// lowerBand = plot(30, title = "Lower Band", linestyle = plot. linestyle_dashed)
// fill(upperBand, lowerBand, color = color.new(color.blue, 95))
// plot(rsi)

// ============= MACD Indicator =============
// Note - In this indicator you have to put the overlay = false in the main indicator function
[macdline, signalline, histline] = ta.macd(close, 12, 26, 9)
plot(macdline, color= color.blue, force_overlay = false)
plot (signalline, color = color.orange, force_overlay = false)
plot(histline, color = histline > 0 ?color.teal : color.red, style = plot.style_columns, force_overlay = false)


// // ============= ATR Indicator =============
// // Note - In this indicator you have to put the overlay = false in the main indicator function
// atr = ta.atr(9)
// plot(atr, color = color.red)

// // ============= Bollinger Band =============
// [middle, upper, lower] =  ta.bb(close, 20, 2)
// upperBand =  plot(upper, color = color.red)
// middleBand =  plot(middle, color = color.blue)
// loweBand =  plot(lower, color = color.green)
// fill(upperBand, loweBand, color = color.new(color.blue, 95))

// ============= Highest and Lowest =============
higgest = ta.highest(close, 20)
lowest = ta.lowest(close, 28)
upperBand =  plot(higgest, force_overlay = true)
LowerBand =  plot (lowest, force_overlay = true)
fill (upperBand, LowerBand, color =  color.new(color.blue, 95))