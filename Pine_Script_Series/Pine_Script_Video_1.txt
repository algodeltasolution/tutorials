// This Pine Script® code is subject to the terms of the Mozilla Public License 2.0 at https://mozilla.org/MPL/2.0/
// © algodeltatv

//@version=6
indicator("Pine Script Video 1", overlay = true)
// plot (open)
// plot (high)
// plot(low)
// plot(close)

// isGreen = close > open
// finalColor color.teal
// if isGreen
    // finalColor color.teal
// else
    // finalColor: color.red
// finalColor = isGreen ?  color.teal: color.red
// plot(volume, style = plot.style_columns, color = color.new(finalColor, 60))

// isBreakout =  close > high[1]
// plotshape(isBreakout, style =  shape.arrowup, color = color.teal, text = "Breakout", location = location.belowbar, textcolor = color.white)

// // Custom EMA Example
// len = input.int(9, minval = 1, title = "Length", inline = "SMA Inline", group = "SMA Group")
// src = input.source(defval = close, title = "Custom SMA Source", tooltip = "SMA Source", inline = "SMA Inline", group = "SMA Group")
// offset = input.int(defval = 0, title = "Offset", minval = -500, maxval = 500, display = display.none)
// out = ta.sma(src, len)
// plot(out, color = color.blue, title = "MA", offset = offset)

out9 = ta.sma(close, 9)
out26 = ta.sma(close, 26)
out50 = ta.sma(close, 50)
plot(out9,title = "MA9" ,color = color.blue)
plot(out26,title = "MA26" ,color = color.blue)
plot(out50,title = "MA50" ,color = color.blue)